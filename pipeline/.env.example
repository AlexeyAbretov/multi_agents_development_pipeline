# Оркестратор + deployer (docker compose из корня репозитория пайплайна).
# Пути DATA_DIR=/data и PROMPTS_DIR=/app/prompts — внутри контейнера.
# Listen-порты: pipeline/ports.env (compose прокидывает PORT в контейнер).
# Локальный npm/VSCode: скопируйте .env.local.example → .env.local (хостовые пути).
POLL_INTERVAL_MS=30000
SCHEDULE_INTERVAL_MS=3600000
# Календарь due_on milestone (IANA). T−1 / T считаются в этой зоне.
SCHEDULE_TZ=Europe/Moscow
GITHUB_TOKEN=
# Целевой продукт (issues/PR, Cursor Cloud клонирует этот репозиторий)
GITHUB_REPO=owner/your-product-repo
CURSOR_API_KEY=
CURSOR_REPO_URL=https://github.com/owner/your-product-repo
CURSOR_STARTING_REF=main
# composer/grok: оркестратор передаёт fast=false. Fast — id с fast.
CURSOR_MODEL=composer-2.5
DATA_DIR=/data
PROMPTS_DIR=/app/prompts
# Compose задаёт mongodb://pipeline-mongo:27017/pipeline (сервис pipeline-mongo).
# Не указывайте localhost: локальный mongod — только в .env.local.
# deployer: stub (по умолчанию) | compose (docker compose продукта)
DEPLOY_MODE=stub
